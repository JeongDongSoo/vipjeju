<IfModule mod_rewrite.c>
    	RewriteEngine On

	RewriteBase /

	RewriteCond $1 !^(index\.php|assets|uploads|images)
	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME} !-d

	RewriteRule ^(.*)$ /djadm/index.php/$1 [L]
</IfModule>